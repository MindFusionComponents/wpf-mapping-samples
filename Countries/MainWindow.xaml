<Window
	x:Class="MindFusion.Mapping.Wpf.Samples.CS.Countries.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:map="http://mindfusion.eu/mapping/wpf"
	mc:Ignorable="d"
	Title="MindFusion.Mapping for WPF Sample: Countries"
	Width="900" Height="700"
	WindowState="Maximized"
	Loaded="MainForm_Load" Icon="/Countries;component/App.ico">

	<Grid x:Name="LayoutRoot" Background="White">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="40" />
		</Grid.RowDefinitions>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="220" />
			</Grid.ColumnDefinitions>

			<!-- display country name in bold above the map -->
			<TextBlock
				Name="lblCountryName"
				Height="24"
				Margin="3"
				VerticalAlignment="Top"
				FontSize="16"
				FontWeight="Bold"
				TextAlignment="Center"/>

			<ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto">
				
				<!-- show the map of currently selected country -->
                <map:MapView Name="mapView">
					<map:MapLayer
                        LineColor="Transparent"
						AutoAssignColors="true"
                        LabelField="NAME_1" />
                </map:MapView>

			</ScrollViewer>

			<!-- display a list of countries -->
			<ListBox
				Name="lbCountries"
				Grid.Column="1"
				Grid.RowSpan="2"
				SelectionChanged="lbCountries_SelectedIndexChanged" />
		</Grid>
		<TextBlock TextWrapping="Wrap" Margin="5" Grid.Row="1" Text="This example shows the administrative division of world countries. You can select a different map from the list on the right.
You can zoom down using the mouse wheel and pan the map by dragging it with left mouse button." />
	</Grid>
</Window>